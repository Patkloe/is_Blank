function is_Blank(s){

 if(s.length === 0)   // if the length property of the string is equal to 0, then the string is empty

    return true;   // empty we return true

 return false;    // otherwise false

}
is_Blank('');
is_Blank('abc');
